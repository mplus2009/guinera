name: Build Release APK (sin firmar)

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Configurar JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Configurar Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Generar Gradle Wrapper si no existe
      run: |
        if [ ! -f "gradlew" ]; then
          echo "📦 Generando Gradle Wrapper..."
          gradle wrapper
        else
          echo "✅ Gradle Wrapper ya existe"
        fi
        chmod +x gradlew

    - name: Compilar APK de Release (sin firmar)
      run: ./gradlew assembleRelease
      env:
        # Forzar a que no intente firmar
        ORG_GRADLE_PROJECT_android_useUnsafeTrackRunner: "true"

    - name: Subir APK compilada (sin firmar)
      uses: actions/upload-artifact@v4
      with:
        name: app-release-unsigned
        path: app/build/outputs/apk/release/*.apk
        retention-days: 30
